apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-kurento
spec:
  storageClassName: "vol-kurento"
  volumeName: vol-kurento-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
status: {}
