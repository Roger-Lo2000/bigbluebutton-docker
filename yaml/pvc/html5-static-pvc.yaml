apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: html5-static
spec:
  storageClassName: "html5-static"
  volumeName: bbb-gke
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
status: {}
