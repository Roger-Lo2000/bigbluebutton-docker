apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ssl-data
spec:
  storageClassName: "ssl-data"
  volumeName: ssl-data-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
status: {}
