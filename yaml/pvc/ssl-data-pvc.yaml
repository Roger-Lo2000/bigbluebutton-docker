apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ssl-data
spec:
  storageClassName: "ssl-data"
  volumeName: bbb-gke
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
status: {}
