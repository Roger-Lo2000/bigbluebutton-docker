apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-mediasoup
spec:
  storageClassName: "vol-mediasoup"
  volumeName: bbb-gke
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
status: {}
