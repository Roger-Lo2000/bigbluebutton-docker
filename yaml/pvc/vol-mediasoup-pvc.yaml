apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-mediasoup
spec:
  volumeName: vol-mediasoup-pv
  storageClassName: "vol-mediasoup"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
status: {}
