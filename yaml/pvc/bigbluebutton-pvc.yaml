apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bigbluebutton
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "bigbluebotton"
  volumeName: bigbluebotton-pv
  resources:
    requests:
      storage: 10Gi
status: {}