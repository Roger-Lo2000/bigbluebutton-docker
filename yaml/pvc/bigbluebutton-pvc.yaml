apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bigbluebutton
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "bigbluebotton"
  volumeName: bbb-gke
  resources:
    requests:
      storage: 10Gi
status: {}