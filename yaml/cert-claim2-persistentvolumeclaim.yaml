apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: cert-claim2
  name: cert-claim2
spec:
  storageClassName: local-storage
  volumeName: cert-pv2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
