apiVersion: v1
kind: Service
metadata:
  name: demo-service1
spec:
  type: NodePort
  ports:
  - port: 48787
    protocol: TCP
    targetPort: 80
  selector:
    app: deployment1
  clusterIP: 5.1.0.30

