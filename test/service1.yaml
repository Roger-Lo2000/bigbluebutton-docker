apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  type: NodePort
  ports:
  - port: 6000
    protocol: TCP
    targetPort: 3000
  selector:
    app: deployment1
  clusterIP: 5.1.0.10