apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  type: NodePort
  ports:
  - port: 3050
    nodePort: 30390
    protocol: TCP
    targetPort: 3000
  selector:
    app: deployment
  clusterIP: 5.1.0.40