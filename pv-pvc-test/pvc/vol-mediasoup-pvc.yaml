apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-mediasoup
spec:
  storageClassName: "vol-mediasoup"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
status: {}
